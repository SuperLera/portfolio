.scroll-btn {
  position: relative;

  &::after {
    content: '';
    position: absolute;
    width: 20px;
    height: 20px;
    background: svg-load('scroll.svg', fill=$text-color, width=100%, height=100%);
  }
}