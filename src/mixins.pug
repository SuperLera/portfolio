mixin icon(name, className)
  - var icon = require(`images/icons/${name}.svg`).default;
  svg(class=className viewBox=icon.viewBox preserveAspectRatio="none")
    use(xlink:href=`sprite.svg#${icon.id.replace('-usage', '')}`)

mixin image(path, className, description, attrRole)
  if role
    img(src=require(`images/${path}`).default class=className role= attrRole)
  else
    img(src=require(`images/${path}`).default class=className alt=description)

//- 1. Если нужен role, а не alt:
//- +image(‘path/file.png’, 'class’, ‘’, ‘presentation’)
//- 2. Если не нужен role, но нужен alt, просто опускаем четверный аргумент:
//- +image(‘path/file.png’, 'class’, ‘Описание’)

mixin socials(mod)
  ul(class='socials socials--' + mod)
    each social in [{title: "insta", link: "https://www.instagram.com"}, {title: "vk", link: "https://vk.com/valeriia_koroleva"}, {title: "github", link: "https://github.com/SuperLera"}]
      li(class='socials__item socials__item--' + mod)
        a(class=`socials__link socials__link--${social.title}` href=`${social.link}` target="_blank")
          +icon(social.title, `socials__icon socials__icon--${mod}`)
